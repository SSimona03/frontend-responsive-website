import React from 'react';
import ReactDom from 'react-dom';
import App from './App';
import './index.css'


ReactDom.render(<App />, document.getElementById('root')); //the App component will be renderd inside the index.html  into a div with Id: root
